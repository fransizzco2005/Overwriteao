# Multi-Channel Discord AI Bot

## Configuration
Channel ID `1546193711942336612` is set in `index.js`.
To add more channels, add them to the `ALLOWED_CHANNELS` array inside `index.js`:

```javascript
const ALLOWED_CHANNELS = [
  '1546193711942336612',
  'ANOTHER_CHANNEL_ID_HERE'
];
```
